NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Multi-tenant: root domain (e.g. crm.example.com or localhost for dev)
NEXT_PUBLIC_ROOT_DOMAIN=localhost

# Dev only: force a specific tenant slug without subdomain
# NEXT_PUBLIC_DEV_TENANT_SLUG=ara

# Required for team invitations (Supabase service role key)
# SUPABASE_SERVICE_ROLE_KEY=
